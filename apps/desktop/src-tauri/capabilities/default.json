{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. The daemon GUI talks to the engine over HTTP/SSE, not Tauri IPC, so no extra grants are needed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
